'use client';


const fazerLogin = async () => {
  //  const da = await fetch('/api/openai').then(e => e.json())
   console.log("askdlasd; lsa")
}

const Login = () => {

  return (
    <>
    <button className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" onClick={fazerLogin}>Login</button>
    </>
  )
}

export default Login